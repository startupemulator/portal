export const state = () => ({
    isMobile: true
})


export const mutations = {
    // toggle(state, todo) {
    //     todo.done = !todo.done
    // }
}
export const actions = {
    // toggle(state, todo) {
    //     todo.done = !todo.done
    // }
}